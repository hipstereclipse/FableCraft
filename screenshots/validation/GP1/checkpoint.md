# GP1 baseline evidence

Audited source: f135546cd170f9c96d1e384576e8fb9a11b6f068 (remote CI success
34709848060, recorded in remote-baseline.json). No gameplay output changed.
All 27 scripts/validate.py commands exited zero. additional-results.json records
explicit lint/spells/syntax and Guild map/roof command exits. Diagnostics have
findings despite successful command execution: see the supplemental defect ledger.

Geometry images are fresh offline renders; names containing cutaway explicitly
remove upper geometry for inspection. route-probe.json is a conservative connected
voxel graph, corroborated with concrete overwritten/missing treads. NPC baseline
probe executes extracted production source with mocked engine handles and records
existing failures. Neither probe asserts engine success. Original-game source
provenance and unresolved conflicts are in the three linked audit documents.

Bedrock executable/session: unavailable in this workspace; all in-engine checks
remain unrun. Full all-category screenshot regeneration is not applicable to this
audit-only pass. The original 45-leaf scoreboard remains unchanged.
