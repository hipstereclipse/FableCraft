# GP2 checkpoint

All 28 scripts/validate.py gates pass in the isolated reviewed-index snapshot.
Explicit lint, spells, ESM syntax, Guild match and roof diagnostic commands pass.
The first extra syntax invocation used Node's CommonJS file mode and rejected
Bedrock's ESM import syntax; first-syntax-runner.log preserves that runner error.
The corrected command parses the same snapshot source via --input-type=module
on stdin, without relying on the inherited untracked scripts/package.json.

The six new route groups also reject actual GP1 geometry. Full all-category
rendering completed; C2 regenerated all 34 registered structure views/provenance.
Only Guild imagery/assets changed. No renderer score is a historical-fidelity
rating. See GUILD_CIRCULATION.md and geometry/ for remaining falls, rails,
adjacent thresholds and unrun engine walking/NPC pathfinding checks.

Validation snapshot excludes parallel uncommitted NPC/portal implementation.
Its provenance is in snapshot-provenance.json. Existing occupied Guilds are not
replaced. The original C3 denominator remains 45 leaves.
