# GP19 reviewed checkpoint

Baseline: GP18 c83246751b86aa489897efa3d0cadd49f303c588; exact-head CI 34733425350 passed.
The reviewed Git index was exported before validation. Only explicit GP19 paths
are staged; concurrent DP6 controller/test work stays outside this checkpoint.
Seven inherited binary hashes remain unchanged and CRLF-only dirt is excluded.

Root reviewed the requester hooks, main/emote/defence integration, fresh resident
resolver, controller transitions, generated native events and the exact Skill JSON
drift. The generator author compared all 51 old/current isolated entity outputs;
only Skill changes. Eight native groups simulate shared component-key deletion,
conditional restoration, two exact player-tag filters, stale marker defence and
Wait retention. Root reviewed those semantics separately from the author.

The independent controller reviewer exercised 13 additional failure cases. Earlier
failures involved exhausted revisions, an invalidation write that could revive a
stale callback, forgotten unverified/offline holder debt and a conflicting new
resident hiding the loaded durable ID. The repaired final source passes 13/13;
its 64-ID overflow quarantine is covered by the independently repeated 16-group
controller suite. See independent-controller-review.md and retained early results.
A separate actual-module/main-adapter integration suite passes 9 groups. The actual
GP18 baseline probe reproduces Wait broadcast and stale fear replacement.

All 51 repository gates and 64 ESM syntax checks pass. The initial maintenance gate
failed because its Might-only fixture lacked the new external reservation binding.
Only that fixture changed; production bytes stayed fixed. The gate was rerun with
its explicit unmanaged boundary. initial-results.json and the first raw failure
are retained alongside the final result and portable rerun script. No other gate
result was overwritten with an unrun or inferred pass.

Fresh C2 and Guild diagnostics pass. All 35 structure assets, all 35 C2 images and
six rendering/data/geometry owners match GP18 exactly. GP18 full-render evidence
is retained from unchanged inputs; no new full render is claimed. Source hashes,
actual commands, generator drift and raw logs are retained here. Probes execute
mocked Bedrock boundaries; they do not execute native pathfinding or physical
movement. Native target caching, collision, multiplayer/reconnect, persistence
and the whole-facility conformance task remain open.

The independent door follow-up reproduced a separate unreadable-ticket history
bug. Its nine cases are observations of the unfixed baseline, not repaired tests.
It is queued for a separate DP6 commit and does not alter GP19's reviewed source.
