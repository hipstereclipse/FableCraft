# DP6 reviewed checkpoint

GP19 baseline: 6f490a7263226ce50e3ad72769ceaa3ad3fa2d24; exact-head CI 34735910068 passed.
Root reviewed the complete door-controller diff and five actual-adapter regression
groups. Availability is checked before the affected player's recovery/return writes.
The private synchronous helper consumes the tick's fresh snapshot; public returns
and deferred clicks still read fresh. Confirmed invalid/absent fallback remains.
Read-only occupancy guards keep per-player error isolation and current allocation.
No generated owner, geometry, main adapter, activity or reward schema changes.

The captured baseline controller is byte-identical to the committed GP19 owner.
The final 38-group suite fails three new regressions against it and passes DP6.
Independent review repeats 38/38 and compares eight old/new failed-write scenarios;
no blocker remains. Baseline observation, red regression and repaired logs remain
separate. The original implementation capture began while HEAD was GP18; its door
owner stayed identical through GP19. Exact loaded hashes preserve that distinction.

All 51 base gates and 64 ESM syntax checks pass in the isolated reviewed-index snapshot.
Fresh 35-asset C2 and Guild diagnostics pass. All 35 structure assets, 35 C2 images and
six rendering/data/geometry owners match GP18 exactly; its full-render evidence is
retained from unchanged inputs, not rerun. All seven inherited binary hashes remain
unchanged; unrelated CRLF noise and local configuration/scratch stay unstaged.

The injected unavailable read leaves a valid backing ticket and functioning write
API. The blocked-default control proves that retaining its exact source changes
successful recovery. This establishes offline handling, not native failure rates,
actual travel/collection, collision, loading, multiplayer or save/crash durability.
Those checks and the broader facility/door-world catalogue remain open.
