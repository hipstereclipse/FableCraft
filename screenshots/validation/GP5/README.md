# GP5 offline evidence

All 38 checks in results.json pass from the reviewed-index final snapshot.
additional-results.json records explicit lint, spells, syntax and Guild diagnostics.
initial-contract-failure/ preserves the first failed run's obsolete C2 registration
expectation; structure-registration-preflight.log and the final base run cover
its owner/manifest correction. No engine checks ran.

The dedicated altar details preserve half slabs; the open cutaways deliberately
remove roof/north wall. Standard full cards draw slabs as cubes. See render-scope.json
and reference-provenance.json for reference limitations and ignored external sources.
geometry-delta.json records 184 surrounding tread changes plus 44 water-rim cells.
The unchanged outside-region geometry hash includes all original altar core,
room anchors and support geometry. Full pipeline counts are in full-render-summary.json.
