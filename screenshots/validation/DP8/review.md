# DP8 reviewed checkpoint

DP7 baseline e13127194b8bd596bf9db9fee03633a413825f24 passed exact-head CI
34739113768. Root reviewed the actual main callback and both runtime owners,
the four new integration groups and the semantic patch. An independent callback
review reported no blocker. The failure proof and final suite execute actual
controllers and generated room voxels; native boundaries remain injected.

All 55 base gates and 65 ESM checks pass in the isolated reviewed-index snapshot.
The final integration suite passes 20 groups. Its same four new groups fail with
only main source replaced by DP7's actual committed main; the other 16 still pass.
Retained red predecessor output is expected evidence. The initial audit's
before-write case independently records twelve missed dwell callbacks, an intact
exact ticket and a successful direct own-family return.

Fresh C2 and Guild diagnostics pass. All 36 structure assets, 36 C2 images and six
rendering/geometry/data owners match DP7 byte-for-byte in the snapshot. Full
282-image evidence is retained from DP7 under these unchanged inputs, not rerun.
Pending GP21 working-tree geometry was excluded by explicit index staging.

Only main's four callback boundaries and the actual integration fixture/tests
change in production/test code. No realm controller, geometry, schema or generated
asset changes are staged. All seven inherited binaries and two untracked local
configurations retain captured hashes. Final documentation was refreshed after
source validation and the 45-leaf scoreboard rerun. Builds remain ignored locally.

Native loading, dwell travel, collision, inventory transfer, multiplayer and
save/reload remain unrun. DP8 isolates exceptions between families; it does not
supply atomic persistence, bounded synchronous call cost or new Guild preparation
receipts. No whole-facility or door-catalogue completion is claimed.
