# Continuation prompt

Continue the FableCraft → TLC Conformance plan on Linux.
Repository: /run/media/eclipse/8E025A94025A80DF/Users/Eclipse/Fablecraft
Branch: phase3-will-destiny
Remote: https://github.com/hipstereclipse/FableCraft.git

Read docs/GUILD_DEMON_PRIORITIES.md first, then docs/CONFORMANCE_PLAN.md and
CONFORMANCE_CHECKLIST.md. The priority ledger is the active execution queue.
The user asks: “Recursively improve the entire guild facility until it feels like
stepping back into the original The Lost Chapters game.” Find additional online
reference photos and make Demon Doors traversable into distinct original reward
worlds. Continue autonomously with separate reviewed milestone commits, validation,
immediate pushes and fresh handoffs; no routine questions.

Current checkpoint: GP23 — taller Guild bridge lamps.
Last resolved commit: 5fcf08a23389760762438c86092a8d3450f96195 (DP10 pushed;
exact-head CI 34770576386 passed).
Containing commit: TLC Conformance — GP23: raise the Guild bridge lamps.
Resolve current hash: git log -1 --format=%H --grep='TLC Conformance — GP23: raise the Guild bridge lamps'.
Never invent a self-hash. Supplemental passes stay in-progress while native
acceptance is unrun; the legacy scoreboard remains 45 leaves and 7 done.

## GP23 current checkpoint

Original TLC view131726186 shows a long dark lamp post beside the capped bridge
end. Eight existing Guild heads rise one block: x58/68,z35/37/53/55,y2 lantern
becomes dark_oak_fence and y3 receives the same standing lantern. Exact height,
positions/count, stock materials and shape are Minecraft adaptations. The
plank_bridge owner changes no RNG call, fixture footprint, deck/slab/bank/water,
rail, anchor or saved-world construction. See GUILD_BRIDGE_LAMPS.md and GP23.
All 56 base gates and 65 ESM checks pass in the isolated reviewed-index snapshot.
Five focused bridge groups and ten independent negative fixtures pass. Actual
DP10/current generators reproduce their respective serialized assets: exactly
16 cells change; all other 395,264 cells, palette/state/metadata, RNG, layout,
anchors, 678 Maze reservations and 981 water cells remain exact. All 13,806
walking nodes, 10,597 reachable nodes and seven complete paths remain. Eight
local routes, 19 protected body sweeps, ten lateral bridge sweeps, five actual
Skill rays and 13 arrival samples/27 refusals retain their results. These are
conservative offline bounds; native collision and passing remain unrun.

Fresh C2, all 282 full-render PNGs, seven Guild documentation scenes and Guild
diagnostics pass. Only Guild changes among the 36 assets/C2 images. Only its
card and places gallery change among 282 full PNGs; only the two wide Guild
views change among seven documentation scenes. Root inspected both cards, the
gallery, both wide views and all eight focused before/after schematics. GP23 is
the next asset/documentation baseline. Only Guild changed among 1,663 worktree
pack files, and all nine protected unrelated files remain exact.

Initial helper errors (inherited CRLF comparison, an untracked full-render PNG
baseline) and focused-image caption spacing were corrected without production
changes; original output remains. Exact raw lint output is retained in JSON;
its readable log drops only the extra trailing blank line. No base gate failed.
Native acceptance is unrun; legacy C3 remains 45 leaves and 7 done.
No occupied Guild is rebuilt; reanchor remains a coordinate refresh.

Seven original TLC references found in DP10 are now indexed:157535182 (map-hall
stair/arch),157299346 (daylight bridge deck),343658414/236178734 (dining hall),
176290219 (adjoining hall/candle ring),27535382 (upper balcony/candle ring),
279597457 (dormitory/candles). Both reference agent and root inspected full pixels.
The supplementary corpus is37, plus the earlier three map images. Matching app
204030 cards and exact direct URLs support original edition. Posting dates:
27535382 March11 2012;279597457 July1 2014 (Steam timezone unknown). Other five
detail pages returned generic Workshop shells. Capture dates, patch/mod/settings,
full dimensions and neutral ordinary Library remain unknown. New bridge image
shows decorated opaque parapets but no lamp and supplies no height/width measure.
External pixels stay ignored in tmp/conformance/next-guild-reference; DP10 and
GP23 textual manifests retain hashes, URLs and exclusions.

DP10's final staged whitespace check reported one blank line at EOF in the raw
lint.log. A shell sequence without fail-fast handling nevertheless committed and
pushed. All functional validators and post-commit source/test/doc whitespace
checks passed. Preserve the raw output/history; GP23/dp10-commit-check-correction.md
records this procedural error. Dependent commit steps must stop on any failure.

## Retained DP10 preparation contract

Independent original-owner/native-effect probes reproduced overwritten late
contents, a late occupant, stale pre/post-write chest handles, wrong item metadata,
intent-only placing recovery, adopted ready/visited history and same-revision
progress replacement. DP10 pins complete job history and adds optional schema1
placing/placed/seeding/seeded preparation receipts in existing Arboretum records.
Fresh volume/occupancy checks follow placement intent; only saved successful
placement permits verification. Seed intent precedes fresh all-slot and history
checks; the live chest is reacquired for exact item/count/name/lore readback.
A seeded receipt permits read-only ready retry; ambiguous intents never replay.
Ready/visited/depleted rooms, exact tickets and source/grid/guards remain.
Only arboretum_doors.js changes among pack inputs; both actual test doubles now
model default empty item lore. See ARBORETUM_PREPARATION.md and DP10 evidence.
All 55 base gates and 65 ESM checks pass in the isolated reviewed-index
snapshot. Runtime 35, actual main integration 20, independent review 22 and
six additional second-review probes pass. Fresh C2 and Guild diagnostics pass;
all 36 structure assets/C2 images and seven visual owners match GP22. Its 282
full PNGs and seven Guild documentation scenes are retained from unchanged
inputs, not rerun. Only the Arboretum controller differs among 1,662 snapshot
pack files. All nine protected unrelated files remain exact. No base gate failed.
A diagnostics helper launch used the wrong working directory; a second-review
fixture initially mistook a source point for a ticket. Both corrections and a
review-helper SHA-logging update are retained; no production fix was required.
No native server or world has run. Legacy C3 remains 45 leaves and 7 done.

## Retained GP22 asset/documentation baseline

Original TLC screenshot 141099874 (Steam app 204030, posted April 24 2013) shows
separate scenic mountain and crenellated tower cutouts ahead of the painted
range panel. Exactly twelve new cells form a shallow mountain at x80..82,z32,
heights 1/2/1, and a tower at x87..89,z33, heights 3/2/3, both from y1. Purple/white
terracotta, light-blue terracotta, the black-wool painted slit, dimensions,
positions and block steps are explicit Minecraft adaptations. Full original
curves, taper, texture, set of props and platform/perimeter remain unresolved.
The z31 board bypass is retained. The earlier forward tower at x89..91,z35
was rejected because it changes the existing east-door route despite reachability.

Independent actual-DP9/current generator and serialized comparisons prove every
other 395,268 cell, preexisting occupied cell/floor, palette/state/metadata,
RNG, layout and anchor exact. Maze stays (46,12,70), with 678 reservations intact.
Only the six decorative ground columns leave the walking graph, 13,812→13,806;
every other node and reachable node stays. Seven complete gate paths retain
identical coordinates. Eight local paths, 19 body sweeps (radius 0.35, height 1.9),
five actual Skill rays, the three-column firing gap and 13 future-arrival samples
with 27 refusals remain. Rays end at z34.5; all new props end at z34 or earlier.
Body/ray checks are conservative offline bounds; decorative tops are not certified
walking floors. Native movement, collision and arrival calibration remain unrun.

Behavior regression passed BEFORE targeted Guild regeneration; only Guild changes
among 1,663 worktree pack files. All 55 base gates and 65 ESM checks pass in the
isolated reviewed-index snapshot. All 12 focused archery groups, independent nine
negative fixtures, second review and Guild diagnostics pass. Fresh C2 records
only the changed Guild asset hash; all 36 distant C2 PNGs remain byte-identical.
All 282 selected full-render PNGs are also identical to GP21 because the new props
are hidden at those angles. Only 26_archery_backboard changes among seven fresh documentation scenes. Root
inspected it and both unchanged wide Guild views, both cards, the gallery and
six focused decoded views.
GP22 supplied the previous asset/documentation baseline; GP21 shares the distant PNGs.

An initial C2-copy helper incorrectly expected changed pixels, then the staging
helper lacked its manifest. A first diagnostics launch used a missing relative
helper path. The first doc comparison also used older retained snapshot images
rather than GP21’s actual custom output; committed-blob comparison corrects the
initial three-image change claim to one. Initial summaries/output and corrections
are retained; no base gate failed.
No production source changed during those corrections. Full/doc renderers simplify
fences as cubes; focused schematics use thin approximate rails/posts. See
GUILD_ARCHERY_SCENERY.md and screenshots/validation/GP22/. No occupied Guild
is rebuilt; reanchor remains coordinate refresh only.

## Retained DP9 contract

Eleven actual-GP21/native-effect probes reproduce destructive placing replay,
late volume/shell changes, silent/wrong seed effects, prefilled containers and
late occupants. The optional room.preparation schema1 phases placing/placed/
seeding/seeded live in the existing fc_dp_guild_v1 record. Existing outer schema,
room phases, exact source/cell/grid and player tickets remain. Every preparation
write compares fresh whole-record authority and reads back its exact value.
The active job pins its original history; unavailable/changed history defers.

A placement intent precedes the native call; only a successful saved placed
receipt permits verification. Unknown legacy placing or intent-only work never
replays. All slots of all four containers must initially be empty. Seed intent
precedes native writes, and every write reacquires its target, checks all slots,
occupancy and fresh history. Fresh post-write containers must match exact item,
amount, name/lore and every unused slot. A seeded receipt permits ready retry
through read-only exact validation; seeding ambiguity never writes again.
Late complete native volume and shell checks reject changes behind sliced scans;
full shell checks also precede cached-room entry. Native API calls use pinned
2.1.0 containsBlock with allowUnloadedChunks:false; failure is not empty volume.

Ready/visited rooms, old records without preparation, depletion, paid/unknown
suppression and exact-ticket recovery remain. No room/Guild rebuild, reseeding,
Arboretum controller change or geometry change. Ambiguous unfinished rooms remain
closed; no manual repair operation is supplied. Separate property/item effects
are not atomic saves, and externally deleting/rolling back history is not solved.

Runtime 29 and actual main adapter 39 groups pass. Independent review passes 34
cases. It found two intermediate defects between item writes (changed history or
new next-container contents); both are fixed, with original findings/logs retained.
Development failures in test expectations/leave-rearm and a Node child-process
Git EPERM in the portable evidence helper are retained and explained. No native
server or world ran. All 55 base gates and 65 ESM checks pass in the isolated reviewed-index snapshot,
with fresh C2 and Guild diagnostics. All 36 structure assets/C2 images and seven
visual owners match GP21; full 282-PNG and Guild documentation renders are
retained from unchanged inputs, not rerun. Only the two handwritten scripts
change among 1,662 snapshot pack files. See LIBRARY_ARCANUM_PREPARATION.md and
screenshots/validation/DP9/ for exact validation results and source hashes.

## Visual and reference baseline

Retain GP21's ten spruce-fence cells at x80..81/x85..92,y1,z38 framing the
three-column firing gap. Its expanded Skill-line sweep accounts for lower-cell
fence height; a continuous divider intersects it despite the block-cell harness.
GP22 preserves the divider, both bridges, targets/hay, marks, doors and roofs.

Before GP23, the supplementary original-TLC corpus was30. GP22 indexes three images found
and inspected during DP9: 91147839 (daylight bridge/tower),350434796 (map hall stairs/
arch/Cullis),3243931107 (elevated courtyard/two bridges). App204030 listings match
the direct image URLs and inspected classic pixels. Detail pages return generic
Workshop shells; posting dates remain unknown. The panorama has conspicuous
camera distortion and cannot establish proportions or camera/mod provenance.
URLs/hashes/observations are retained in DP9/reference-followup.* and
GP22/reference-index.json; pixels/HTML stay ignored in tmp/conformance/dp9-reference/.
Six dated-2009 classic exteriors remain uncounted pending exact-edition proof.
Production GP22 scenery relies only on 141099874, not the newly indexed images.

## Next three actions

1. Continue the dining comparison in GP23/dining-followup.*. Original343658414/
   236178734 show separate short red stools; current x38/x42,y1,z36..47 has
   24 contiguous oak-stair seats. A scratch removal of12 alternating seats
   preserves every old walk/reachable node, adds12 and retains checked routes.
   It does not establish original seat count or round stool shape. Author any
   chosen seating/table/lighting change through its owner with final geometry,
   circulation, body and source/RNG comparison; no GP24 is authored yet. Original
   twin dining stairs remain a larger unresolved layout issue.
2. Revisit bridges, map-hall stair/arch, range platform and adjoining interiors
   against the37 indexed references. Full-cube parapets leave a one-block opening
   without proving native passing clearance; uniform widening covers bank routes
   and conflicts with the z54 bollard. Stock trapdoors are mutable and lack pinned
   collider evidence. Do not raise the engine target for an unverified custom
   parapet. Retain GP23 lamps, fixtures, water/RNG and saved builds. Neutral ordinary
   Library, complete dormitory bed count/size and Maze gallery proportions remain
   missing; crops, glow and the distorted panorama cannot supply dimensions.
3. Purposeful Skill arrival needs native offset/search/event calibration;
   preserve the DP6 13-sample/27-refusal proposal and GP19 activity authority.
   Do not retry unchanged failed official pinned-server transport. The next-door
   audit recommends Greatwood Caves → The Butterfly House → Cutlass Bluetane:
   retained 2005 guide pages 47/174 verify active multiplier ≥14, pairing and reward.
   The source and Legendary item are both absent. Never relabel Hobbe Cave or
   replace the legacy Warrior/Harbinger contract. DP10/next-door-audit.md inventories
   strict current multiplier authority and every third-family integration; raw
   string/fractional values and nonhostile hits pass legacy checks. New canonical
   admission needs its own strict reader; source/room/item construction and full
   cross-family guard/ticket/return tests remain unimplemented. Source/interior images
   are only 214×123 and do not establish full geometry. See GP22/next-door-audit.md
   and next-door-provenance.json; primary pixels stay ignored. This is proposed,
   not an implemented third world. Native acceptance remains open for both pilots.

## Retained owners and compatibility

DP7(e13127194b8bd596bf9db9fee03633a413825f24) implements new registered Greatwood
Gorges → Arboretum, with full evil or 10 completed witnessed Crunchy Chick uses,
a distinct 49×28×49 woodland, one physical shared Wellow's Pickhammer and exact
source-instance return. Separate source receipt/witness/index/cell history and
bounded allocation begin (620000,272,600000), disjoint from Guild (600000,272,600000).
Preserve all eight legacy personas, old unregistered Gorges, Guild history and
Nostro. Original murder parity and native consumption/crash durability are open.
DP8(2d56f97e325642c936b7ad4898efff09981bb926) isolates both families' maintenance/
ticks and reporting; five/forty-tick cadence stays exact. DP3/DP5/DP6 preserve old
occupied-ticket return/protections and unavailable-ticket authority. Tickets
never authorize reconstructing room history or replenishing rewards.

GP19 owns requester-specific Follow/Wait only for skill_range/skill_hall with
journal+witness, outstanding tag cleanup, exact resident authority and defence/
relationship/training arbitration. No automatic Follow resume after reload.
GP17 owns deferred relationship checks; GP16 owns live Skill target/hay/full-ray
preflight. GP18 adds 11 Maze study cells; GP20 recolors 10 dormitory partition cells.
GP2/GP13 routes, GP3 training, GP4 defence, GP5 cave manifest, GP6 identities,
GP7 retired sweeps, GP8/GP14 Chamber, GP9 Will, GP10 map, GP11 arches and GP12
Library remain. Read their ledger/docs before touching the corresponding owner.

## Validation, repository and release hazards

No Bedrock engine has run. Lighting, fluids, collision/stairs, native movement,
Follow target caching, Wait/interruption, multiplayer/reload, portal travel,
collection and saved-world acceptance remain unrun. Render metrics do not prove
canon fidelity/playability. C2 has 36 structures; full cards include 33 structures
and 282 PNGs total. GP22 seven Guild doc scenes are the applicable visual input.

After EVERY checklist edit run python scripts/conformance_score.py --write then
--check. Never change 45 leaves/7 done or infer completion from supplemental passes.
Run scripts/tests/test_gen_behavior.py BEFORE regeneration. Use targeted owners;
never hand-edit generated BP/RP entities/models/textures/animations/controllers/
attachables/particles/HUD/data/registry/language outputs. Validate an isolated
reviewed-index snapshot with python scripts/validate.py --output screenshots/validation/<ID>.
Review helpers and source hashes; preserve original failed output. Build archives
stay ignored in tmp/builds/faithful-* and never get staged or published.

Hundreds of apparent edits are inherited CRLF noise. Never git add -A, commit -a,
renormalize, stash/reset/discard unrelated work or add .gitattributes. Review
--ignore-cr-at-eol, stage explicit named semantic patches via git apply --cached
--ignore-whitespace, then staged diff --check with core.whitespace=cr-at-eol.
Preserve three dist archives, four scripts/_align/overlay_*.png, ignored scratch,
untracked .eslintrc.json and packs/Fablecraft_BP/scripts/package.json. Hashes are
in tmp/conformance/gp16/protected-baseline.json and GP22/provenance.json.
Never run obsolete patch_guild_tiling.py, global Git config or force push.

Spells remain separately complete at ccafd4c. L4 release is blocked by naming debt,
unselected public title and missing saved-world remap. Wayfarer Tales is provisional;
original-only public build needs zero-debt scan. Free; no extracted Fable pack
assets or monetization. Other POI/player-quest gaps remain in their detailed docs.

Git writes/network require escalation. The user authorizes immediate pushes to
verified origin, with commit-scoped user.name=hipstereclipse and
user.email=davidbteta@gmail.com. Verify pushed exact-commit CI via gh run list/view.
Report failures honestly; preserve local commits. At context limits print this
continuation verbatim; continue independent work while native/reference access
is unavailable.
